
public class DataType2 {

	/*
	 * 자바의 데이터 유형 정수 : byte(1byte), char(2byte), short(2byte), int(4byte),
	 * long(8byte) byte : 통신, 이미지 파일, -128(2^7)~ 0 ~ 127(2^7-1), 부호 비트가 있다. char :
	 * 문자(다국어) unicode '\u0000' ~ '\uffff', 0 ~ 65535 short : 2 byte -2^15 ~ 2^15-1
	 * int : 4 byte(32bit) - default long : 8 byte(64bit) -> C언어는 4byte -> long long
	 * (8byte) 리터럴 'l'또는 'L'을 써준다. ex) 123L C언어는 사용하는 CPU의 비트 수에 따라사 int가 16비트 (8비트
	 * 컴퓨터: 아두이노) 또는 32비트다. 실수 : float(4byte), double(8byte) 오차가 있을 수 있음. 왜냐하면 기수,
	 * 지수로 표현하는 비트 수의 제약이 있으므로 default : double -> 오차가 float보다 작다. (표현되는 비트 수가 많으므로)
	 * 논리형 : boolean (1byte). true 또는 false 조건을 만족하는가? 아니면 만족하지 않는가?
	 * 
	 * 
	 */
	public static void main(String[] args) {

	}

}
